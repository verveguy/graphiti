[project]
name = "graphiti-otel-stdout-example"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "graphiti-core",
    "real-ladybug>=0.15.0,<0.16.0",
    "opentelemetry-api>=1.20.0",
    "opentelemetry-sdk>=1.20.0",
]

[tool.uv.sources]
graphiti-core = { path = "../..", editable = true }
